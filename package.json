{
  "name": "action-local-cache",
  "version": "1.1.0",
  "private": true,
  "description": "A Github Action to save and restore files across job runs directly in the runner's file system",
  "main": "dist/main/index.ts",
  "engines": {
    "node": "^16.13.0"
  },
  "scripts": {
    "all": "npm run format && npm run lint && npm run type-check && npm run pack",
    "format": "prettier --write **/*",
    "lint": "prettier --check --loglevel warn **/*",
    "pack": "ncc build src/main.ts --out dist/main && ncc build src/post.ts --out dist/post",
    "type-check": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/techmatt101/action-local-cache.git"
  },
  "keywords": [
    "actions",
    "node",
    "self-hosted",
    "runner"
  ],
  "author": "techmatt101",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/io": "^1.0.2",
    "loglevel": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "@vercel/ncc": "^0.36.1",
    "prettier": "^2.2.1",
    "typescript": "^4.5.2"
  }
}
